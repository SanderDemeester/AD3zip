#include "header/huffman.h"
#include "stdio.h"

void standaard_huffman(char *input_buffer, int lengte, int actie){
  char *freq_tabel = (char*) malloc(sizeof(char)*255);

  if(actie){
    //encodeer
  }else{
    //decodeer
  }
}
